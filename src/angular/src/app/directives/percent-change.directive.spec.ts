import { PercentChangeDirective } from './percent-change.directive';

describe('PercentChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentChangeDirective();
    expect(directive).toBeTruthy();
  });
});
