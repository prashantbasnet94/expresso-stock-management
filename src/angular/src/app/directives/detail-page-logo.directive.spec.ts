import { DetailPageLogoDirective } from './detail-page-logo.directive';

describe('DetailPageLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailPageLogoDirective();
    expect(directive).toBeTruthy();
  });
});
