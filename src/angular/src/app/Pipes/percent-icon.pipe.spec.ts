import { PercentIconPipe } from './percent-icon.pipe';

describe('PercentIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentIconPipe();
    expect(pipe).toBeTruthy();
  });
});
